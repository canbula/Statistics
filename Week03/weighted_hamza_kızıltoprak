from week02 import data
import random
def weighted_srs(data, n, weights, with_replacement = False):
    sample = random.choices(data, weights=weights, k=n) if with_replacement else random.sample(data, n)
    return sample
